import { useNavigate } from 'react-router-dom';
import { useAppSession } from '../context/AppSessionContext';

const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo', 'Junio', 'Julio', 'Agosto', 'Septiembre', 'Octubre', 'Noviembre', 'Diciembre'];
export function PeriodoSelectPage() { const navigate = useNavigate(); const { empresa, ejercicio, selectPeriodo } = useAppSession(); return <div className="flow-shell"><div className="flow-card panel setup-wizard"><div className="setup-progress" aria-label="Progreso de configuración"><span className="is-complete"><b>1</b> Empresa</span><i /><span className="is-complete"><b>2</b> Ejercicio</span><i /><span className="is-current"><b>3</b> Período</span></div><span className="eyebrow">Paso 3 de 3 · Ejercicio {ejercicio}</span><h1>Elige el período activo</h1><p>El período activo controlará las operaciones y reportes del ejercicio.</p><div className="period-grid">{meses.map((mes, index) => <button className="selection-card" key={mes} onClick={() => { selectPeriodo(index + 1); navigate('/'); }}><strong>{String(index + 1).padStart(2, '0')} · {mes}</strong><span>{empresa?.razonSocial}</span></button>)}</div></div></div>; }
