import { useNavigate } from 'react-router-dom';
import { useAppSession } from '../context/AppSessionContext';

export function EjercicioSelectPage() { const navigate = useNavigate(); const { empresa, selectEjercicio } = useAppSession(); const actual = new Date().getFullYear(); const ejercicios = [actual, actual - 1, actual - 2]; return <div className="flow-shell"><div className="flow-card panel setup-wizard"><div className="setup-progress" aria-label="Progreso de configuración"><span className="is-complete"><b>1</b> Empresa</span><i /><span className="is-current"><b>2</b> Ejercicio</span><i /><span><b>3</b> Período</span></div><span className="eyebrow">Paso 2 de 3 · {empresa?.razonSocial}</span><h1>Elige el ejercicio fiscal</h1><p>Selecciona el año fiscal en el que registrarás y consultarás las operaciones.</p><div className="selection-grid">{ejercicios.map(ejercicio => <button className="selection-card" key={ejercicio} onClick={() => { selectEjercicio(ejercicio); navigate('/seleccion/periodo'); }}><strong>Ejercicio {ejercicio}</strong><span>Período fiscal anual</span></button>)}</div></div></div>; }
